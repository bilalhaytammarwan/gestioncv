spring:
  application:
    name: User-Token
  data:
    mongodb:

      host: localhost
      port: 27019
      database: mongodb_container
      username: admin
      password: admin
      authentication-database: admin
      auto-index-creation: true

  cloud:
    config:
      uri: http://localhost:8888
  config:
    import: optional:configserver:http://localhost:8888




jwt:
  Hmac-secret: "CqARLzJ/cU6OYYqMAC33pRIrcHX6jRRQ176WoCZ34K4="

server:
  port: 8228
candidate:
  service:
    url: http://localhost:8888

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

