spring:
  cloud:
    config:
      uri: http://localhost:8888
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: send-email-service
#  data:
#    mongodb:
#      host: mongo
#      port: 27017
#      database: sendEmail-service
#      username: admin
#      password: admin
#      authentication-database: admin
#      auto-index-creation: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: "reactudemy722@gmail.com"
    password: "sctk gnhj nczj ornh"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8668
