spring:
  application:
    name: Opportunity-service
  data:
    mongodb:
      host: localhost
      port: 27019
      database: opportunity_service_DB
      username: admin
      password: admin
      authentication-database: admin
      auto-index-creation: true
server:
  port: 8338
candidate:
  service:
    url: http://localhost:8228/api/candidate
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

