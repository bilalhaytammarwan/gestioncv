services:
  search-service-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8448:8448"
    networks:
      - microservices-net

networks:
  microservices-net:
    external: true